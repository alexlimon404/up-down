# Дамп таблицы users и восстановление на другом сервере

## 0. Работа с дампами через DataGrip (GUI)

### Создание дампа таблицы users через DataGrip

#### Способ 1: Экспорт через контекстное меню
1. **Откройте DataGrip** и подключитесь к базе данных
2. В дереве Database Explorer найдите таблицу `users`
3. **Правый клик** на таблице `users` → **SQL Scripts** → **SQL Generator**
4. Выберите опции:
   - ✅ Generate INSERT statements (если нужны данные)
   - ✅ Generate CREATE statements (если нужна структура)
5. Нажмите **Copy to Clipboard** или **Save to File**
6. Сохраните как `users_dump.sql`

#### Способ 2: Экспорт данных (Data Extractor)
1. **Правый клик** на таблице `users` → **Export Data** → **SQL Inserts**
2. В диалоге настройте:
   - Format: SQL Inserts
   - File name: `users_data.sql`
   - Options:
     - ✅ Add table creation statement (если нужна структура)
     - ✅ Add DROP table statement (если нужно пересоздать)
3. Нажмите **OK**

#### Способ 3: Использование встроенного pg_dump
1. Откройте **Database** → **Import/Export**
2. Выберите **Export with 'pg_dump'**
3. Настройте параметры:
   ```
   Connection: [ваше подключение]
   Database: [ваша база]
   Output file: users_dump.dump
   Format: Custom
   Objects: users (выберите только таблицу users)
   ```
4. Нажмите **Run**

#### Способ 4: Через SQL Console (если есть доступ к серверу)
1. Откройте **SQL Console** (Ctrl+Shift+F10)
2. Выполните команду:
   ```sql
   -- Создать дамп через COPY
   COPY users TO '/tmp/users_data.csv' WITH (FORMAT CSV, HEADER);
   ```
   или
   ```sql
   -- Экспорт в SQL формат
   COPY (SELECT * FROM users) TO STDOUT WITH (FORMAT text);
   ```

### Восстановление дампа через DataGrip

#### Способ 1: Выполнение SQL скрипта
1. **File** → **Open** → выберите файл `users_dump.sql`
2. Откроется SQL редактор с содержимым дампа
3. Проверьте подключение к нужной базе (верхний правый угол)
4. Нажмите **Execute** (Ctrl+Enter) или **Run** (зеленая стрелка)
5. Дождитесь завершения

#### Способ 2: Import через контекстное меню
1. **Правый клик** на базе данных → **SQL Scripts** → **Run SQL Script**
2. Выберите файл `users_dump.sql`
3. Нажмите **OK**

#### Способ 3: Import Data (для CSV/JSON)
1. **Правый клик** на базе данных → **Import Data from File**
2. Выберите файл с данными
3. DataGrip автоматически определит структуру
4. Настройте mapping столбцов
5. Нажмите **Import**

#### Способ 4: Использование встроенного pg_restore
1. Откройте **Database** → **Import/Export**
2. Выберите **Import with 'pg_restore'**
3. Настройте параметры:
   ```
   Connection: [ваше подключение]
   Input file: users_dump.dump
   Database: [целевая база]
   Options:
     ✅ Clean before restore (очистить перед восстановлением)
     ✅ Create DB (если нужно создать базу)
   ```
4. Нажмите **Run**

### Копирование таблицы между базами в DataGrip

#### Прямое копирование (если обе базы доступны)
1. Откройте обе базы данных в DataGrip
2. **Правый клик** на таблице `users` в источнике
3. **Copy Table to...** → выберите целевую базу данных
4. Настройте опции:
   - ✅ Copy data
   - ✅ Copy indexes
   - ✅ Copy foreign keys
5. Нажмите **OK**

### Полезные горячие клавиши DataGrip
- `Ctrl+Shift+F10` - открыть SQL Console
- `Ctrl+Enter` - выполнить текущий запрос
- `Ctrl+Alt+L` - форматировать SQL
- `F4` - открыть редактор таблицы
- `Ctrl+F12` - быстрый поиск по структуре

### Настройка автоматического бэкапа в DataGrip

1. **Tools** → **Database** → **Backup**
2. Создайте новую конфигурацию бэкапа
3. Настройте расписание (если нужно)
4. Выберите таблицы для бэкапа
5. Укажите путь сохранения

---

## 🔴 Устранение проблем: "0 rows saved"

### Причина 1: Активен фильтр на таблице
**Проверка:**
1. Откройте таблицу `users` (двойной клик или F4)
2. Посмотрите на верхнюю панель - там может быть активный фильтр
3. Если видите значок 🔍 или "Filter" - это причина

**Решение:**
- Нажмите на иконку фильтра и выберите **"Remove Filter"** или **"Reset"**
- Или используйте `Ctrl+Alt+R` для сброса фильтров

### Причина 2: Экспортируется только выделение
**Проверка:**
- При экспорте была выбрана опция "Selected rows only"
- Но ничего не было выделено в таблице

**Решение:**
1. Откройте таблицу `users`
2. Выделите все строки (`Ctrl+A`)
3. Правый клик → **Export Data**
4. Убедитесь, что выбрано **"All rows"** вместо "Selected rows"

### Причина 3: Таблица действительно пустая
**Проверка через SQL Console:**
```sql
SELECT COUNT(*) FROM users;
```

Если результат `0` - таблица пустая.

**Проверка в DataGrip:**
1. Откройте таблицу `users` (F4)
2. Внизу должно быть написано количество строк: "1 of 1000" или "No rows"

### Причина 4: Проблемы с правами доступа
**Проверка:**
```sql
-- Проверить права на таблицу
SELECT grantee, privilege_type
FROM information_schema.role_table_grants
WHERE table_name='users';

-- Попробовать прочитать данные
SELECT * FROM users LIMIT 10;
```

**Решение:**
```sql
-- Дать права на чтение (выполнить от суперпользователя)
GRANT SELECT ON users TO your_username;
```

### Причина 5: Неправильная схема (schema)
**Проверка:**
- Возможно таблица находится в другой схеме (не public)

**Решение:**
```sql
-- Найти таблицу users во всех схемах
SELECT table_schema, table_name,
       (SELECT COUNT(*) FROM information_schema.tables t2
        WHERE t2.table_schema = t1.table_schema
        AND t2.table_name = 'users') as count
FROM information_schema.tables t1
WHERE table_name = 'users';

-- Указать схему явно при экспорте
SELECT * FROM schema_name.users;
```

### Причина 6: Ограничение LIMIT в настройках DataGrip
**Проверка:**
1. **File** → **Settings** (Ctrl+Alt+S)
2. **Database** → **Data Editor**
3. Найдите **"Limit page size to"**

**Решение:**
- Увеличьте лимит или снимите галочку
- Или в SQL Console используйте свой запрос без лимита

### Причина 7: Проблема с кодировкой или специальными символами
**Решение:**
1. При экспорте выберите другой формат:
   - Попробуйте **CSV** вместо SQL
   - Попробуйте **JSON**
2. Измените кодировку на UTF-8

### 💡 Рекомендуемый способ экспорта через DataGrip:

**Вариант 1: Через SQL Console (самый надежный)**
```sql
-- 1. Проверить количество строк
SELECT COUNT(*) FROM users;

-- 2. Посмотреть первые строки
SELECT * FROM users LIMIT 10;

-- 3. Экспортировать через контекстное меню результата
-- Выполните запрос SELECT * FROM users;
-- Затем правый клик на результате → Export Data → SQL Inserts
```

**Вариант 2: Через Database Tools**
1. В Database Explorer выберите таблицу `users`
2. **Правый клик** → **Dump Data to File**
3. Выберите формат и путь
4. Проверьте лог экспорта внизу экрана

**Вариант 3: Использовать pg_dump из DataGrip**
1. **Tools** → **Database Tools** → **Manage Data Sources**
2. Выберите ваше подключение
3. Перейдите на вкладку **Options**
4. Найдите путь к `pg_dump`
5. Используйте встроенный экспорт:
   - **Database** → **Export with 'pg_dump'**

### 🔍 Диагностика через лог DataGrip

1. **Help** → **Show Log in Finder/Explorer**
2. Откройте файл `idea.log`
3. Найдите ошибки, связанные с экспортом
4. Поищите строки со словами "export", "users", "error"

### ✅ Проверочный чеклист

- [ ] Таблица не пустая (`SELECT COUNT(*)`)
- [ ] Нет активных фильтров
- [ ] Выбрано "All rows", а не "Selected rows"
- [ ] Есть права SELECT на таблицу
- [ ] Правильная схема (schema)
- [ ] Нет ограничений LIMIT в настройках
- [ ] Попробован экспорт через SQL Console

---

## 1. Создание дампа таблицы users (Консоль)

### Только структура таблицы (без данных)
```bash
pg_dump -h localhost -U postgres -d database_name -t users --schema-only -F c -f users_schema.dump
```

### Только данные (без структуры)
```bash
pg_dump -h localhost -U postgres -d database_name -t users --data-only -F c -f users_data.dump
```

### Структура + данные (полный дамп таблицы)
```bash
pg_dump -h localhost -U postgres -d database_name -t users -F c -f users_full.dump
```

### Дамп в SQL формате (текстовый, легко читается)
```bash
pg_dump -h localhost -U postgres -d database_name -t users -f users.sql
```

### Параметры:
- `-h` - хост базы данных (localhost, IP адрес)
- `-U` - имя пользователя PostgreSQL
- `-d` - имя базы данных
- `-t` - имя таблицы для дампа
- `-F c` - формат custom (сжатый, бинарный)
- `-F p` - формат plain (обычный SQL, по умолчанию)
- `--schema-only` - только структура
- `--data-only` - только данные
- `-f` - выходной файл

### Пример с конкретными данными
```bash
# Замените на свои значения
pg_dump -h localhost -U postgres -d mydb -t users -F c -f users_$(date +%Y%m%d_%H%M%S).dump
```

---

## 2. Перенос дампа на другой сервер

### Вариант 1: SCP (через SSH)
```bash
scp users_full.dump user@remote-server:/path/to/destination/
```

### Вариант 2: rsync (с прогрессом)
```bash
rsync -avhP users_full.dump user@remote-server:/path/to/destination/
```

### Вариант 3: Если файл большой - сжать перед отправкой
```bash
# Сжать
gzip users_full.dump

# Отправить
scp users_full.dump.gz user@remote-server:/path/to/destination/

# На удаленном сервере распаковать
gunzip users_full.dump.gz
```

---

## 3. Восстановление дампа на новом сервере

### Восстановление из custom формата (.dump)
```bash
pg_restore -h localhost -U postgres -d target_database -t users users_full.dump
```

### Восстановление из SQL файла
```bash
psql -h localhost -U postgres -d target_database -f users.sql
```

### Восстановление с очисткой существующих данных
```bash
# Сначала удалить таблицу users, если существует
psql -h localhost -U postgres -d target_database -c "DROP TABLE IF EXISTS users CASCADE;"

# Затем восстановить
pg_restore -h localhost -U postgres -d target_database users_full.dump
```

### Восстановление только данных (таблица уже существует)
```bash
pg_restore -h localhost -U postgres -d target_database --data-only -t users users_data.dump
```

### Восстановление с verbose режимом (показывает прогресс)
```bash
pg_restore -h localhost -U postgres -d target_database -v -t users users_full.dump
```

---

## 4. Полный пример: Дамп + Перенос + Восстановление

### На исходном сервере:
```bash
# 1. Создать дамп
pg_dump -h localhost -U postgres -d production_db -t users -F c -f users_backup.dump

# 2. Проверить размер файла
ls -lh users_backup.dump

# 3. Отправить на новый сервер
scp users_backup.dump user@new-server:/tmp/
```

### На новом сервере:
```bash
# 1. Проверить что файл пришел
ls -lh /tmp/users_backup.dump

# 2. Создать базу данных если нужно
psql -U postgres -c "CREATE DATABASE new_database;"

# 3. Восстановить таблицу
pg_restore -h localhost -U postgres -d new_database -v /tmp/users_backup.dump

# 4. Проверить количество записей
psql -U postgres -d new_database -c "SELECT COUNT(*) FROM users;"
```

---

## 5. Дополнительные полезные команды

### Проверить структуру таблицы
```bash
psql -h localhost -U postgres -d database_name -c "\d users"
```

### Проверить количество записей
```bash
psql -h localhost -U postgres -d database_name -c "SELECT COUNT(*) FROM users;"
```

### Посмотреть первые 10 записей
```bash
psql -h localhost -U postgres -d database_name -c "SELECT * FROM users LIMIT 10;"
```

### Создать дамп с фильтрацией данных (WHERE)
```bash
# Сначала создать SQL запрос
echo "COPY (SELECT * FROM users WHERE created_at > '2024-01-01') TO STDOUT" | \
psql -h localhost -U postgres -d database_name > users_filtered.sql
```

---

## 6. Автоматический бэкап (cron)

### Создать скрипт backup.sh:
```bash
#!/bin/bash
BACKUP_DIR="/backups"
DB_NAME="production_db"
DB_USER="postgres"
DATE=$(date +%Y%m%d_%H%M%S)

# Создать директорию если не существует
mkdir -p $BACKUP_DIR

# Создать дамп
pg_dump -h localhost -U $DB_USER -d $DB_NAME -t users -F c -f $BACKUP_DIR/users_$DATE.dump

# Удалить старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "users_*.dump" -mtime +7 -delete

echo "Backup completed: users_$DATE.dump"
```

### Добавить в crontab (каждый день в 2:00):
```bash
crontab -e

# Добавить строку:
0 2 * * * /path/to/backup.sh >> /var/log/users_backup.log 2>&1
```

---

## 7. Устранение проблем

### Ошибка: "relation users does not exist"
```bash
# Проверить список таблиц
psql -h localhost -U postgres -d database_name -c "\dt"
```

### Ошибка: "permission denied"
```bash
# Дать права пользователю
psql -U postgres -d database_name -c "GRANT ALL PRIVILEGES ON TABLE users TO your_user;"
```

### Ошибка при восстановлении: "already exists"
```bash
# Использовать флаг --clean для удаления перед восстановлением
pg_restore -h localhost -U postgres -d database_name --clean -t users users_full.dump
```

---

## 8. Переменные окружения для автоматизации

```bash
# Создать файл .pgpass для автоматической авторизации
echo "localhost:5432:database_name:postgres:your_password" > ~/.pgpass
chmod 600 ~/.pgpass

# Теперь можно не указывать пароль в командах
pg_dump -h localhost -U postgres -d database_name -t users -f users.dump
```

---

## Полезные ссылки
- [PostgreSQL pg_dump документация](https://www.postgresql.org/docs/current/app-pgdump.html)
- [PostgreSQL pg_restore документация](https://www.postgresql.org/docs/current/app-pgrestore.html)
